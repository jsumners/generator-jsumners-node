{
  "name": "<%= appName %>",
  "version": "0.1.0",
  "description": "<%= description %>",
  "homepage": "https://example.com/<%= appName %>",
  "main": "<%= mainFile %>",
  "scripts": {
    "test": "gulp test"
  }<% if (keywords) { %>,
  "keywords": [<%= keywords %>],<% } %>
  "author": "<%- author %>",
  "license": "MIT"<% if (addGit) { %>,
  "repository": {
    "type": "git",
    "url": "<%= gitUrl %>"
  }
  <% } %>
}